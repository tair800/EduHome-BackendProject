@* 
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <a class="btn btn-outline-github" asp-action="create">
                Create
            </a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>

                            <th>
                                Image
                            </th>
                            <th>
                                Name
                            </th>
                            <th>
                                Date
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var blog in Model)
                        {
                            <tr>
                                <td>
                                    <img src="~/img/blog/@blog.ImgUrl" alt="" />
                                </td>
                                <td>
                                    @blog.Name
                                </td>
                                <td>
                                    @blog.Date?.ToString("dd.MMMM.yyyy")
                                </td>

                                <td>
                                    <a class="btn btn-outline-facebook" asp-action="detail" asp-route-id="@blog.Id"><i class="mdi mdi-account-card-details"></i></a>
                                    <a class="btn btn-outline-primary" asp-action="update" asp-route-id="@blog.Id"><i class="mdi mdi-settings"></i></a>
                                    <a class="btn btn-outline-danger" asp-action="delete" asp-route-id="@blog.Id"><i class="mdi mdi-delete"></i></a>
                                </td>

                            </tr>

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

 *@